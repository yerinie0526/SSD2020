<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dongduk.cs.ssd.summerpetstore.dao.repository.mapper.MarketMapper">   
  <cache />
  
	<!--장터에 물품 등록하기-->
	<insert id="registerItem" parameterType="dongduk.cs.ssd.summerpetstore.controller.MarketForm" >
		<selectKey keyProperty="itemId" resultType="int" order="BEFORE">
			SELECT item_seq.nextval AS itemid FROM DUAL
		</selectKey>
		INSERT INTO item
		VALUES (#{itemId}, 'in progress', #{itemKind}, #{name}, #{price}, #{description}, 0, 'yerine', #{cname})
	</insert>
	
	<!--장터 물품 자세히 보기-->
	<select id="showInfo" resultType="dongduk.cs.ssd.summerpetstore.model.ItemModel">
		SELECT itemid, iname AS name, itemkind, price, idescription AS description, istatus
		FROM item
		WHERE itemid = #{value}
	</select>
	
	<!--장터 물품 삭제-->
	<delete id="deleteItem">
		DELETE FROM item
		WHERE itemId = #{itemId}
	</delete>
	
	<!--장터물품 수정 -->
	<update id="updateItem" >
		UPDATE Item
		SET itemKind=#{itemKind}, iname=#{iname}, price=#{price}, description=#{description}, productId=#{productId}
		WHERE itemId=#{itemId}
	</update>
	
	<!--장터상태 수정-->
	<update id="updateItemStatus" >
		UPDATE Item
		SET istatus=#{istatus}
		WHERE itemId=#{itemId}
	</update>
	
	<!--장터검색-->
	<select id="searchItem" resultType="dongduk.cs.ssd.summerpetstore.model.ItemModel">
		SELECT itemid as itemId, iname as name, price
		FROM Item
			<!-- <if test='param1 != null'>
				cname=#{param1}
			</if>
			<if test='param1 != null && param2 != null'>
				cname=#{param1} AND itemkind=#{param2}
			</if>
			<if test="param1 == null && param2 != null">
				itemkind=#{param2}
			</if>
			<if test='param1 != null && param2 != null && param3 != null'>
				cname=#{param1} AND itemkind=#{param2} AND lower(name) like #{param3}
			</if>
			<if test="param1 == null && param2 == null && param3 != null">
				lower(name) like #{param3}
			</if>
			<if test="param1 == null && param2 != null && param3 != null">
				itemkind=#{param2} AND lower(name) like #{param3}
			</if>
			<if test="param1 != null && param2 == null && param3 != null">
				cname=#{param1} AND lower(name) like #{param3}
			</if>-->
		
		
	</select>

	

</mapper>